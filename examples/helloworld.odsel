
/* A very simple q+a hello world in odsel! */

import odsel;

: main() {
    odsel::inputf("What is your name?\n", myvar);
    odsel::printf("Well, %s, this is the (infamous) Hello, World!\n",myvar);
};

main();

